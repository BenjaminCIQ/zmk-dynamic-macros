# Baseline: the dynamic-macro module is NOT instantiated (built with
# -DDM_SIZE_BASELINE so the behavior node is absent and the Kconfig auto-enable
# stays off). This firmware's size is the floor every other row subtracts from.
